// const canvas = document.querySelector("canvas");
// const context = canvas.getContext("2d");

// let data = {
//   x: 10,
//   y: 10,
//   width: 50,
//   height: 50,
//   fillStyle: "red",
// };

// let a = 1;
// let b = 1;

// function update() {
//   if (data.x + data.width > canvas.width || data.x < 0) {
//     // henc kpni patin heta galis horizonakan
//     a = -1;
//   }
//   if (data.x + data.height > canvas.height || data.x < 0) {
//     // henc kpni patin heta galis uxxahayac
//     b = -1;
//   }
//   data.x = data.x + a; // mi hat aja tanum
//   data.y = data.y + b; // kpnuma patin u sharnakuma
// }

// function drow() {
//   context.clearRect(0, 0, canvas.width, canvas.height); // hin@ jnjuma nor tazen nkaruma
//   context.fillStyle = data.fillStyle; // sarquma karmir
//   context.fillRect(data.x, data.y, data.width, data.height); // nkaruma qarakusi
// }

// function loop() {
//   requestAnimationFrame(loop); // anverj nuyn@ nkaruma
//   update();
//   drow();
// }
debugger;
